{% extends 'base.html.twig' %}

{% block title %}Rocket Bet | Edition{% endblock %}

{% block body %}

  <div class="row">
    <div class="col-md-12 article-nav">
      <a href="{{ url('article', { 'id': article.id } ) }}" class="retour-nav"><i class="fas fa-arrow-left"></i>&nbsp;Retour</a>

      {% if is_granted('ROLE_ADMIN') or article.author.id == app.user.id %}

        {{ include('article/_delete_form.html.twig') }}

      {% endif %}
    </div>
  </div>

  <div class="row article-new">
    <div class="col-md-12">

      {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}

    </div>
  </div>

{% endblock %}
