{% extends 'base.html.twig' %}

{% set title = 'BetRocket | Login' %}

{% block body %}

  <div class="jumbotron jumbotron-bg">
    <h1 class="display-4">Connexion</h1>

    <hr class="my-4">

    {% if error %}
      <div><i class="fas fa-exclamation-triangle"></i>&nbsp;{{ error.messageKey|trans(error.messageData, 'security')
        }}</div>
    {% endif %}

    <form action="{{ path('login') }}" method="post">

      <div class="form-group">
        <label for="username">Adresse email:</label>
        <input type="text" class="form-control" id="username" name="_username" aria-describedby="emailHelp"
               placeholder="Adresse email" value="{{ last_username }}"/>
        <small id="emailHelp" class="form-text text-muted">Ne divulguez jamais vos identifiants de connexion</small>
      </div>

      <div class="form-group">
        <label for="password">Mot de passe:</label>
        <input type="password" class="form-control" id="password" placeholder="Password" name="_password">
      </div>

      <a href="{{ url('registration') }}" class="blue-link">> S'inscrire</a><br/>
      <a href="{{ url('password') }}" class="blue-link">> Mot de passe oublié</a><br/><br/>

      <button type="submit" class="btn btn-primary">Connexion</button>

    </form>

  </div>

{% endblock %}